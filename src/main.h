#ifndef __MAIN_H__
#define __MAIN_H__

#include "../include/mainpage.h"
#include "../include/startpage.h"

#include "graphics.h"
#include "iostream"
#include <conio.h>     

// 获取屏幕的宽度和高度
const int screenWidth = GetSystemMetrics(SM_CXSCREEN) * 0.8;  // 屏幕宽度
const int screenHeight = GetSystemMetrics(SM_CYSCREEN) * 0.8; // 屏幕高度

#endif 